apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: #VALUE0#
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: #VALUE1#
  storageClassName: #VALUE2#
  volumeMode: Filesystem
  volumeName: #VALUE3#
